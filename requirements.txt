# WP Launcher — Python runtime dependencies
# Pinned to exact versions. Dependabot will bump them via PRs.

# Web framework
Flask==3.0.0
Werkzeug==3.1.8
Flask-WTF==1.2.2

# WebSocket / real-time
Flask-SocketIO==5.4.0
python-socketio==5.16.2
# 0.33.3 importait distutils et cassait sur Python 3.12+ : Socket.IO
# retombait silencieusement en async_mode 'threading'. 0.40.x rétablit le
# worker eventlet, que scripts/start.sh sélectionne automatiquement.
eventlet==0.40.3
# Transport WebSocket quand on tourne en mode 'threading' (worker gthread) —
# sans lui les clients dégradent en long-polling.
simple-websocket==1.1.0

# I18n
Flask-Babel==4.0.0

# Utilities
chardet==7.4.3
python-dotenv==1.0.0
psutil==5.9.8
requests==2.33.1  # GitHub OAuth service, HTTP calls

# Deployments: SSH client + at-rest encryption for stored private keys
# paramiko 4.x drops DSA (DSSKey) support — handled in app/services/ssh_service.py
paramiko==4.0.0
cryptography==50.0.0

# WSGI / production
# <26 volontairement : gunicorn 26 supprime le worker eventlet, que
# scripts/start.sh sélectionne en priorité (sinon 'Invalid worker class'
# et boucle de redémarrage systemd).
gunicorn==25.3.0
