# WP Launcher — Python runtime dependencies
# Pinned to exact versions. Dependabot will bump them via PRs.

# Web framework
Flask==3.0.0
Werkzeug==3.1.6
Flask-WTF==1.2.2

# WebSocket / real-time
Flask-SocketIO==5.4.0
python-socketio==5.11.0
eventlet==0.33.3

# I18n
Flask-Babel==4.0.0

# Utilities
chardet==5.2.0
python-dotenv==1.0.0
psutil==5.9.8
requests==2.32.3  # GitHub OAuth service, HTTP calls

# WSGI / production
gunicorn==21.2.0
